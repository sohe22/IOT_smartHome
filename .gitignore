# 1. 파이썬 필수 제외 (캐시 파일)
__pycache__/
*.py[cod]
*$py.class

# 2. 데이터베이스 및 중요 데이터 파일 (보안/용량)
*.db
*.sqlite3
*.csv
*.xlsx
*.log

# 3. VS Code 설정 (개인 설정 제외)
.vscode/

# 4. 운영체제 생성 파일 (맥/윈도우 썸네일 등)
.DS_Store
Thumbs.db

# 5. 아두이노/펌웨어 빌드 결과물 (소스코드만 올리기 위해)
# 만약 Firmware 폴더 안에 빌드 폴더가 생긴다면 추가하세요 (예: build/)
*.hex
*.elf
*.bin

# 6. (혹시 모를) 가상환경 폴더
# 미니콘다를 쓰시지만, 혹시 나중에 venv를 만들 수도 있으니 남겨둬도 좋습니다.
venv/
env/
.env

# 7. 기타 제외하고 싶은 폴더가 있다면 아래에 추가 (예: 데이터 폴더)
# data/